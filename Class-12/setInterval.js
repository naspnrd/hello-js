// setInterval
// setInterval(callback_function, interval, ...arguments);

// let count = 0;
// const intervalId = setInterval(() => {
//   count++;
//   console.log(`Interval count: ${count}`);
//   if (count === 5) {
//     clearInterval(intervalId);
//   }
// }, 1000);

// let start = 1;
// function countUp(name) {
//   // start -> parameters // local scope
//   console.log(`${name} says the count is ${start}`); // 1
//   start++; // 2
// }
// const intervalId = setInterval(countUp, 1000, "Neeraj");
